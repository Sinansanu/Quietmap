# =============================================================================
# Frontend Environment Variables (Vercel / Production)
# =============================================================================

# Deployed FastAPI Backend URL
# Leave blank or omit for local development (uses Vite proxy to http://localhost:8000)
# DO NOT put DATABASE_URL here - the frontend must never touch the database directly!
VITE_API_URL=https://your-backend-deployment.vercel.app
